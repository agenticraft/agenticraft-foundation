name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install ${{ matrix.python-version }}

      - name: Install dependencies
        run: uv sync --group dev

      - name: Run tests
        run: uv run pytest tests/ -v --tb=short

  release:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install uv
        uses: astral-sh/setup-uv@v4

      - name: Set up Python
        run: uv python install 3.12

      - name: Install dependencies
        run: uv sync --group dev

      - name: pip-audit
        run: |
          uv pip compile pyproject.toml -q -o /tmp/requirements.txt
          uvx pip-audit -r /tmp/requirements.txt

      - name: Build package
        run: uv build

      - name: Verify package
        run: uvx twine check dist/*

      - name: Check wheel contents
        run: uvx check-wheel-contents dist/*.whl

      # ``cyclonedx-py`` dropped the bare ``--format`` interface; the current
      # CLI takes a SOURCE subcommand. ``environment`` documents a Python
      # environment — so the wheel is installed into a throwaway venv first
      # and the SBOM is generated against THAT interpreter. Running it against
      # uvx's own ephemeral venv would produce a syntactically valid SBOM
      # describing the SBOM tool instead of the release, which is worse than
      # the loud failure the old flags now produce.
      - name: Generate SBOM
        run: |
          uv venv sbom-env
          uv pip install --python sbom-env/bin/python dist/*.whl
          uvx --from cyclonedx-bom cyclonedx-py environment \
            sbom-env/bin/python --of JSON -o sbom.json

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            dist/*.whl
            dist/*.tar.gz
            sbom.json
          generate_release_notes: true
          draft: false
          prerelease: ${{ contains(github.ref, 'alpha') || contains(github.ref, 'beta') || contains(github.ref, 'rc') }}

      - name: Publish to Test PyPI
        if: contains(github.ref, 'alpha') || contains(github.ref, 'beta')
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          repository-url: https://test.pypi.org/legacy/

      - name: Publish to PyPI
        if: "!contains(github.ref, 'alpha') && !contains(github.ref, 'beta')"
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          skip-existing: true
